@startuml
start
:创建 DI 容器;
:扫描插件和插件 UI 程序集（核心模式跳过）;
:校验 manifest;
if (兼容?) then (是)
  :注册插件服务;
else (否)
  :记录 Blocked;
endif
:连接核心数据库;
:执行核心迁移;
:按插件创建数据库扩展（核心模式跳过）;
:探测实际插件 schema（核心模式跳过）;
:执行插件迁移（核心模式跳过）;
:加载核心共享数据;
:插件生成实例注册项（核心模式跳过）;
:创建实例、UI 贡献和模板贡献（核心模式跳过）;
:创建主窗口;
stop
@enduml
