# UI 视觉样式设计

## 1. 目标

DiaryApp 保留深色工作台和蓝色交互色，通过少量明确的页面模板统一标题、间距、按钮语义和状态表达。
视觉统一不要求所有页面使用相同结构：记录、统计和 Tracker 管理属于高密度工作台，查询、调查和脚本管理属于标准功能页，设置和编辑器属于模态对话框。

本设计只调整视觉层级和交互语义，不改变数据库、脚本、Tracker 或远程同步行为。

## 2. 页面模板

### 2.1 标准功能页

适用于工作项查询、团队调查和脚本工作台：

- 使用白色一级标题和灰色副标题；
- 页面主操作位于标题区右侧；
- 条件、摘要等独立功能块使用统一卡片；
- 页面边距、标题与首个内容区的距离保持一致。

### 2.2 紧凑工作台

适用于日记、统计和 Tracker 管理：

- 优先保留数据和编辑空间，不强制增加大面积标题区；
- 顶部工具栏或页签使用统一高度、字号和图标尺寸；
- 搜索框、CheckBox、按钮和分页文字在同一工具栏内垂直居中；
- 大型表格保持平铺，独立配置分组才使用卡片。

### 2.3 模态对话框

适用于程序设置、标签、模板、数据库、脚本运行和 Tracker 设置：

- 普通标题使用白色，不使用红色或绿色表达展开、选中等常规状态；
- 副标题使用较低对比度并允许换行；
- 底部操作区右对齐，顺序为中性操作在前、主操作在后；
- 红色只用于删除、清空数据和明确的不可逆放弃操作。

## 3. 基础样式

公共样式位于 `Diary.GUIBase/Resources/AppStyles.axaml`：

| Class | 用途 |
| --- | --- |
| `PageTitle` | 标准页面或设置窗口一级标题 |
| `PageSubtitle` | 一级标题下方的简短说明 |
| `Header` | 需要居中的传统对话框标题，逐步迁移到统一颜色和字号 |
| `DialogFooter` | 右对齐的模态操作区 |
| `EmptyStateTitle` | 完整空状态或错误状态标题 |
| `EmptyStateDescription` | 空状态或错误状态说明 |
| `ToolbarCheckBox` | 工具栏中的 CheckBox 垂直对齐 |
| `PaginationText` | 分页区域文字垂直对齐 |
| `WorkspaceSection` | 工作台中的可展开内容分组 |
| `WorkspaceToolbar` | 工作台中的搜索、筛选和分页工具栏 |

设置项继续复用 `SettingLineBox`、`SettingLineCaption` 和 `SLV`。设置分组展开时只使用蓝色强调，不使用成功色；内容分隔线使用普通边框色，不使用危险色。

## 4. 操作颜色语义

| 语义 | 样式 | 示例 |
| --- | --- | --- |
| 主操作 | `Primary` | 保存、创建、运行、查询、迁移 |
| 中性操作 | 默认按钮 | 取消、关闭、返回、重新加载 |
| 危险操作 | `Danger` | 删除、删除数据、清空、覆盖不可恢复内容 |

“禁用”是否使用危险样式取决于数据是否保留。保留数据且可以立即恢复的启停操作保持中性；会删除本地或远程数据的操作必须使用 `Danger`。

## 5. 文案规范

- 产品名称统一写作 `Redmine`，代码类型名暂不重命名；
- 凭据字段统一写作 `API Key`；
- “关闭”只表示退出页面，“删除”或“删除数据”明确表示数据影响；
- 错误提示应同时描述问题和下一步操作。

## 6. 自动化验收

CDP 视觉回归至少覆盖 1280×800 深色主题：

- 工具栏控件中心线偏差不超过 2px；
- 设置窗口最后一组仍为“AI 与 MCP”；
- 对话框底部操作区完整可见；
- DataGrid 操作列不被裁剪；
- 空状态、错误状态和只读状态没有重叠；
- Redmine 样式检查只执行读取和页面导航，不创建 Issue、不导入问题、不提交工时。

## 7. 迁移顺序

1. 收敛公共样式、按钮语义、Redmine 命名和明确的对齐问题；
2. 迁移程序设置和旧式标题对话框；
3. 统一日记、统计和 Redmine 工作台；
4. 补充只读 CDP 回归、用户手册截图和最终视觉复检。

## 8. 当前实现状态

- 公共标题、说明、空状态、对话框操作区和工具栏对齐 Class 已建立；
- 程序设置已改为白色标题和中性关闭操作，设置分组不再使用绿色或危险色；
- 工作项查询、团队调查和脚本工作台已复用统一页面标题；
- 数据库异常、调查禁用、脚本列表和数据模板已复用统一状态样式；
- 标签、模板、附加字段、Tracker 设置、数据库配置和迁移对话框已统一标题或底部操作区；
- Redmine 工作台页签、可展开分组、搜索筛选工具栏和分页区域已统一；
- `ui-redmine-style` 只读套件覆盖 Tracker 配置、插件状态、基本信息、问题管理和项目管理截图，并断言两个筛选 CheckBox 与搜索框的中心线偏差不超过 2px；
- 该只读套件不保存配置，不同步服务器定义，不搜索或导入 Issue，不创建 Issue，也不提交工时。
- 2026-08-23 已在 Linux X11 1280×800 下完成设置、smoke、核心、扩展、脚本编辑器、Survey、数据库异常、附加字段和 Redmine 只读视觉复检，并更新程序设置、标签、模板和 Tracker/Redmine 用户手册截图。
